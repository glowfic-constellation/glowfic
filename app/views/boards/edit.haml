- content_for :breadcrumbs do
  = link_to "Continuities", continuities_path
  &raquo;
  = link_to @continuity.name, continuity_path(@continuity)
  &raquo;
  %b Edit

= form_for @continuity, url: continuity_path(@continuity), method: :put do |f|
  %table.form-table
    %thead
      %tr
        %th.editor-title{colspan: 2} Edit Continuity
    = render 'editor', f: f

%br
%br

- content_for :reorder_title do
  Organize Continuity Sections
  = link_to new_board_section_path params: { board_id: @continuity.id } do
    .link-box.action-new + New Section
= render 'reorder_sections', sections: @board_sections

- if @continuity.ordered?
  %br
  = render 'reorder_posts', unsectioned_posts: @unsectioned_posts
