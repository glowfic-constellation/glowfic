-# locals: ( f:, gallery: )

%div
  .sub= f.label :name
  %div{class: cycle('even', 'odd')}= f.text_field :name, placeholder: "Gallery Name"
%div
  .sub.vtop= f.label :gallery_group_ids, 'Groups'
  .select2-field{class: cycle('even', 'odd')}= tag_select(gallery, f, :gallery_groups)
