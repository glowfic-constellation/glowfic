- content_for :breadcrumbs do
  = link_to "Continuities", boards_path
  &raquo;
  = link_to @board_section.board.name, board_path(@board_section.board)
  &raquo;
  %b= @board_section.name

- content_for :posts_title do
  = @board_section.name
  - if @board_section.board.editable_by?(current_user)
    = link_to edit_board_section_path(@board_section) do
      .link-box.blue
        = image_tag '/images/pencil.png'
        Edit
    = link_to board_section_path(@board_section), :method => :delete, confirm: 'Are you sure you want to delete this template?' do
      .link-box.red x Delete

= render partial: 'posts/list', locals: {posts: @posts, hide_continuity: true, path: :board_section_path}
