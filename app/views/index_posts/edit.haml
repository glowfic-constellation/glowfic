- content_for :breadcrumbs do
  = link_to "Indexes", indexes_path
  &raquo;
  = link_to @index_post.index.name, @index_post.index
  &raquo;
  = link_to @index_post.post.subject, post_path(@index_post.post)

= form_with model: @index_post do |f|
  .form-table#index-post-editor
    .editor-title Edit Post In Index
    %div
      .sub Index
      .even
        = link_to @index_post.index.name, @index_post.index
    - if @index_post.index_section.present?
      %div
        .sub Index Section
        .odd
          = link_to @index_post.index_section.name, @index_post.index_section
    %div
      .sub Post
      .even= link_to @index_post.post.subject, post_path(@index_post.post)
    %div
      .sub.vtop= f.label :description
      .odd= f.text_area :description
    .form-table-ender= f.submit "Save", class: 'button'
