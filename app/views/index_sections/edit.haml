- content_for :breadcrumbs do
  = link_to "Indexes", indexes_path
  &raquo;
  = link_to @section.index.name, @section.index
  &raquo;
  %b Edit

= form_with model: @section, method: :put do |f|
  .form-table#index-section-editor
    .editor-title Edit Index Section
    = render 'editor', f: f
