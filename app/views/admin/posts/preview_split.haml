- content_for :breadcrumbs do
  = link_to "Admin Tools", admin_path
  &raquo;
  = link_to "Post Split", split_posts_path
  &raquo;
  Preview

Splitting
= link_to @reply.post.subject, @reply.post
at reply id #
= params[:reply_id]
\. New Post:

.content-header= params[:subject]
- @reply.id = nil
= render 'replies/single', reply: @reply, hide_footer: true

= render 'split'
