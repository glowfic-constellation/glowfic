name: reviewdog
on:
  push:
    branches: [ master ]
jobs:
  call_linters:
    uses: marri/glowfic/.github/workflows/reviewdog.yml@v1
    with:
      reporter: github-check
      filter_mode: nofilter
