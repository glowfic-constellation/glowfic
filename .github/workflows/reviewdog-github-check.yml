name: reviewdog
on:
  push:
    branches: [ master ]
jobs:
  call_linters:
    uses: marri/glowfic/.github/workflows/reviewdog.yml@add/reviewdog
    with:
      reporter: github-check
      filter_mode: nofilter
