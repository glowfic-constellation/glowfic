name: reviewdog
on:
  push:
    branches: [ master ]
jobs:
  reviewdog:
    uses: marri/glowfic/.github/workflows/reviewdog.yml@${{ github.ref_name }}
    with:
      reporter: github-check
      filter_mode: nofilter
